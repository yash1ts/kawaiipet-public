# Copy to keystore.properties at repo root (gitignored) and fill in.
# Generate keystore: keytool -genkeypair -v -keystore release.keystore -alias YOUR_ALIAS \
#   -keyalg RSA -keysize 2048 -validity 10000
storeFile=release.keystore
storePassword=
keyAlias=kawaiipet
keyPassword=
